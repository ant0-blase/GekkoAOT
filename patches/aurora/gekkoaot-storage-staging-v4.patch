diff --git a/lib/gfx/encoding.cpp b/lib/gfx/encoding.cpp
--- a/lib/gfx/encoding.cpp
+++ b/lib/gfx/encoding.cpp
@@ -358,8 +358,9 @@ bool needs_staging_copy(const FramePacket& frame, const FrameOp& op) {
   const auto& highWater = op.highWater;
   if (highWater.verts > frame.copied.verts || highWater.uniforms > frame.copied.uniforms ||
-      highWater.indices > frame.copied.indices || highWater.storage > frame.copied.storage) {
+      highWater.indices > frame.copied.indices || highWater.storageSegment != frame.copied.storageSegment ||
+      highWater.storage > frame.copied.storage) {
     return true;
   }
   if constexpr (UseTextureBuffer) {
@@ -377,8 +378,28 @@ void copy_staging_to_high_water(wgpu::CommandEncoder& cmd, FramePacket& frame, c
   copy_staging_buffer_range(cmd, frame, frame.copied.uniforms, highWater.uniforms, UniformStagingOffset,
                             res.uniformBuffer);
   copy_staging_buffer_range(cmd, frame, frame.copied.indices, highWater.indices, IndexStagingOffset, res.indexBuffer);
-  copy_staging_buffer_range(cmd, frame, frame.copied.storage, highWater.storage, StorageStagingOffset,
-                            res.storageBuffer);
+
+  if (highWater.storageSegment != frame.copied.storageSegment) {
+    frame.copied.storageSegment = highWater.storageSegment;
+    frame.copied.storage = 0;
+  }
+  if (highWater.storage > frame.copied.storage) {
+    const wgpu::Buffer* source = nullptr;
+    uint64_t sourceOffset = 0;
+    if (highWater.storageSegment == 0) {
+      source = &staging_buffer(frame.stagingBuffer);
+      sourceOffset = StorageStagingOffset;
+    } else {
+      // The recorder can append another overflow segment while this pass is
+      // encoded on the worker. Read only the source captured with the op.
+      source = &op.storageSource;
+    }
+    const uint32_t copyStart = align_down_copy_offset(frame.copied.storage);
+    const uint32_t copyEnd = AURORA_ALIGN(highWater.storage, 4);
+    cmd.CopyBufferToBuffer(*source, sourceOffset + copyStart, res.storageBuffer, copyStart, copyEnd - copyStart);
+    frame.copied.storage = highWater.storage;
+  }

   if constexpr (UseTextureBuffer) {
     for (size_t i = frame.copied.textureUploadCount; i < op.textureUploads.size(); ++i) {
diff --git a/lib/gfx/frame.cpp b/lib/gfx/frame.cpp
--- a/lib/gfx/frame.cpp
+++ b/lib/gfx/frame.cpp
@@ -684,6 +684,16 @@ void end_frame(EndFrameCallback callback) {
     auto& packet = g_framePackets[frameSlot];
     g_stagingBuffers[stagingSlot].Unmap();
     g_mappingStates[stagingSlot].store(BufferMapState::Unmapped, std::memory_order_release);
+    for (auto& segment : packet.storageOverflow) {
+      if (segment.mapped) {
+        segment.buffer.Unmap();
+        segment.mapped = false;
+      }
+    }
+    // Keep overflow staging buffers alive until the command encoder has been
+    // submitted by the host callback. The encoded copies retain them as GPU
+    // sources even though the FramePacket itself can now be recycled.
+    auto storageOverflow = std::move(packet.storageOverflow);
     auto encoder = std::move(packet.encoder);
     const auto stats = packet.stats;
     auto afterSubmitCallbacks = std::move(packet.afterSubmitCallbacks);
@@ -700,6 +710,7 @@ void end_frame(EndFrameCallback callback) {
     if (callback) {
       callback(encoder, std::move(afterSubmitCallbacks));
     }
+    (void)storageOverflow;
     g_frameSlots.release(frameSlot);
     expire_cached_bind_groups();
     map_staging_buffer(stagingSlot, true);
diff --git a/lib/gfx/frame_packet.hpp b/lib/gfx/frame_packet.hpp
--- a/lib/gfx/frame_packet.hpp
+++ b/lib/gfx/frame_packet.hpp
@@ -17,6 +17,7 @@ struct StagingHighWater {
   uint32_t uniforms = 0;
   uint32_t indices = 0;
   uint32_t storage = 0;
+  uint32_t storageSegment = 0;
   uint32_t textureUpload = 0;
   size_t textureUploadCount = 0;
 };
@@ -180,9 +181,16 @@ struct FrameOp {
   EncoderTask* encoderTask = nullptr;
   StagingHighWater highWater;
   std::vector<const TextureUpload*> textureUploads;
+  wgpu::Buffer storageSource;
 };

 using RenderPassList = std::deque<RenderPass>;
-
+
+struct StorageStagingSegment {
+  wgpu::Buffer buffer;
+  ByteBuffer data;
+  bool mapped = false;
+};
+
 struct FramePacket {
   RenderPassList renderPasses;
   std::deque<TextureCopy> textureCopies;
@@ -197,6 +204,8 @@ struct FramePacket {
   ByteBuffer uniforms;
   ByteBuffer indices;
   ByteBuffer storage;
+  std::deque<StorageStagingSegment> storageOverflow;
+  uint32_t storageSegment = 0;
   ByteBuffer textureUpload;
   wgpu::CommandEncoder encoder;
   std::vector<AfterSubmitCallback> afterSubmitCallbacks;
diff --git a/lib/gfx/recording.cpp b/lib/gfx/recording.cpp
--- a/lib/gfx/recording.cpp
+++ b/lib/gfx/recording.cpp
@@ -10,6 +10,7 @@
 #include "tex_palette_conv.hpp"
 #include "texture.hpp"
 #include "../gx/fifo.hpp"
+#include "../gx/command_processor.hpp"
 #include "../gx/gx.hpp"
 #include "../gx/pipeline.hpp"
 #ifdef AURORA_ENABLE_RMLUI
@@ -67,6 +68,8 @@ struct FrameRecorder {
 };

 FrameRecorder g_recorder;
+constexpr size_t GekkoAOTMiB = 1024u * 1024u;
+size_t g_gekkoaotStorageGlobalHighWaterMiB = 0;

 std::string pass_label(std::string_view kind) {
 #ifdef AURORA_GFX_DEBUG_GROUPS
@@ -230,12 +233,31 @@ FramePacket& current_frame_packet() { return g_recorder.frame(); }

 RenderPassList& current_render_passes() { return g_recorder.passes(); }

+ByteBuffer& current_storage(FramePacket& frame) {
+  if (frame.storageSegment == 0) {
+    return frame.storage;
+  }
+  const size_t index = static_cast<size_t>(frame.storageSegment - 1u);
+  AURORA_ASSERT(index < frame.storageOverflow.size(), "storage segment {} is out of range", frame.storageSegment);
+  return frame.storageOverflow[index].data;
+}
+
+const ByteBuffer& current_storage(const FramePacket& frame) {
+  if (frame.storageSegment == 0) {
+    return frame.storage;
+  }
+  const size_t index = static_cast<size_t>(frame.storageSegment - 1u);
+  AURORA_ASSERT(index < frame.storageOverflow.size(), "storage segment {} is out of range", frame.storageSegment);
+  return frame.storageOverflow[index].data;
+}
+
 StagingHighWater current_high_water(const FramePacket& frame) noexcept {
   return {
       .verts = static_cast<uint32_t>(frame.verts.size()),
       .uniforms = static_cast<uint32_t>(frame.uniforms.size()),
       .indices = static_cast<uint32_t>(frame.indices.size()),
-      .storage = static_cast<uint32_t>(frame.storage.size()),
+      .storage = static_cast<uint32_t>(current_storage(frame).size()),
+      .storageSegment = frame.storageSegment,
       .textureUpload = static_cast<uint32_t>(frame.textureUpload.size()),
       .textureUploadCount = frame.textureUploads.size(),
   };
@@ -255,6 +278,11 @@ FrameOp capture_frame_op(FramePacket& frame, FrameOpType type, uint32_t index) {
       .highWater = current_high_water(frame),
   };
+  if (op.highWater.storageSegment != 0) {
+    const size_t segmentIndex = static_cast<size_t>(op.highWater.storageSegment - 1u);
+    AURORA_ASSERT(segmentIndex < frame.storageOverflow.size(), "storage segment {} is out of range", op.highWater.storageSegment);
+    op.storageSource = frame.storageOverflow[segmentIndex].buffer;
+  }
   op.textureUploads.reserve(op.highWater.textureUploadCount);
   for (size_t i = 0; i < op.highWater.textureUploadCount; ++i) {
     op.textureUploads.push_back(&frame.textureUploads[i]);
@@ -424,7 +446,7 @@ void enqueue_pass(FramePacket& frame, uint32_t passIndex);

 void resume_efb_pass_loading(const RenderPass& prevPass) {
   RenderPass newPass{
-      .label = pass_label("EFB"),
+      .label = prevPass.label.empty() ? pass_label(g_recorder.inOffscreen ? "Offscreen" : "EFB") : prevPass.label,
       .colorAttachments = prevPass.colorAttachments,
       .colorAttachmentCount = prevPass.colorAttachmentCount,
       .depthStencilView = prevPass.depthStencilView,
@@ -600,7 +622,11 @@ RecordedFrame end_recording() {
   frame.stats.lastVertSize = frame.verts.size();
   frame.stats.lastUniformSize = frame.uniforms.size();
   frame.stats.lastIndexSize = frame.indices.size();
-  frame.stats.lastStorageSize = frame.storage.size();
+  size_t storageBytes = frame.storage.size();
+  for (const auto& segment : frame.storageOverflow) {
+    storageBytes += segment.data.size();
+  }
+  frame.stats.lastStorageSize = storageBytes;
   frame.stats.lastTextureUploadSize = frame.textureUpload.size();

   for (auto& array : gx::g_gxState.arrays) {
@@ -1195,12 +1221,102 @@ Range push_uniform(const uint8_t* data, size_t length) {
   return push(current_frame_packet().uniforms, data, length, resources().limits.minUniformBufferOffsetAlignment);
 }

+size_t storage_size() noexcept {
+  if (!g_recorder.active()) {
+    return 0;
+  }
+  return current_storage(current_frame_packet()).size();
+}
+
+size_t storage_capacity() noexcept { return static_cast<size_t>(StorageBufferSize); }
+
+size_t storage_alignment() noexcept {
+  return static_cast<size_t>(resources().limits.minStorageBufferOffsetAlignment);
+}
+
+bool rollover_storage_pass(size_t requiredEnd) {
+  if (!g_recorder.active() || g_recorder.currentRenderPass == UINT32_MAX) {
+    Log.error("GEKKOAOT_AURORA_STORAGE_V4 phase=rollover-rejected reason=no-active-pass required={}", requiredEnd);
+    return false;
+  }
+
+  auto& frame = current_frame_packet();
+  const size_t previousSegment = frame.storageSegment;
+  const size_t previousUsed = current_storage(frame).size();
+
+  const wgpu::BufferDescriptor descriptor{
+      .label = "GekkoAOT Overflow Storage Staging",
+      .usage = wgpu::BufferUsage::MapWrite | wgpu::BufferUsage::CopySrc,
+      .size = StorageBufferSize,
+      .mappedAtCreation = true,
+  };
+  StorageStagingSegment segment;
+  segment.buffer = webgpu::g_device.CreateBuffer(&descriptor);
+  auto* mapped = static_cast<uint8_t*>(segment.buffer.GetMappedRange(0, StorageBufferSize));
+  if (mapped == nullptr) {
+    Log.error("GEKKOAOT_AURORA_STORAGE_V4 phase=rollover-rejected reason=map-failed required={}", requiredEnd);
+    return false;
+  }
+  segment.data = ByteBuffer{mapped, static_cast<size_t>(StorageBufferSize)};
+  segment.mapped = true;
+
+  auto& prevPass = current_render_passes()[g_recorder.currentRenderPass];
+  prevPass.discardable = !prevPass.has_consumer() && !prevPass.has_content();
+  enqueue_pass(frame, g_recorder.currentRenderPass);
+  resume_efb_pass_loading(prevPass);
+
+  frame.storageOverflow.emplace_back(std::move(segment));
+  frame.storageSegment = static_cast<uint32_t>(frame.storageOverflow.size());
+
+  // Every cached storage Range is relative to the current GPU storage contents.
+  // A new segment reuses offsets from zero, so no cached Range may cross this
+  // pass boundary. Pipeline objects remain valid; only data-backed ranges are
+  // invalidated and rebuilt by the next draw.
+  for (auto& array : gx::g_gxState.arrays) {
+    array.cachedRange = {};
+  }
+  gx::fifo::clear_draw_cache();
+  gx::g_gxState.dirty |= gx::DirtyTextures | gx::DirtyUniform | gx::DirtyImmediates;
+
+  Log.warn("GEKKOAOT_AURORA_STORAGE_V4 phase=rollover slot={} frame={} draw={} from_segment={} to_segment={} "
+           "used={} triggering_required={} segment_capacity={}",
+           g_recorder.frameSlot, frame.frameId, g_recorder.drawCallCount, previousSegment, frame.storageSegment,
+           previousUsed, requiredEnd, StorageBufferSize);
+  return true;
+}
+
 Range push_storage(const uint8_t* data, size_t length) {
   ZoneScoped;
   if (!check_recording("push_storage")) {
     return {};
   }
-  return push(current_frame_packet().storage, data, length, resources().limits.minStorageBufferOffsetAlignment);
+  auto& frame = current_frame_packet();
+  auto& storage = current_storage(frame);
+  const size_t alignment = storage_alignment();
+  const size_t current = storage.size();
+  const size_t aligned = alignment != 0 ? AURORA_ALIGN(current, alignment) : current;
+  const size_t required = aligned + length;
+  const size_t requiredMiB = (required + GekkoAOTMiB - 1u) / GekkoAOTMiB;
+
+  if (requiredMiB > g_gekkoaotStorageGlobalHighWaterMiB) {
+    g_gekkoaotStorageGlobalHighWaterMiB = requiredMiB;
+    if (required > 32u * GekkoAOTMiB) {
+      Log.warn("GEKKOAOT_AURORA_STORAGE_V4 phase=highwater slot={} frame={} draw={} segment={} current={} "
+               "incoming={} required={} capacity={} required_mib={}",
+               g_recorder.frameSlot, frame.frameId, g_recorder.drawCallCount, frame.storageSegment, current, length,
+               required, StorageBufferSize, requiredMiB);
+    }
+  }
+
+  if (required > StorageBufferSize) {
+    Log.error("GEKKOAOT_AURORA_STORAGE_V4 phase=unexpected-overflow slot={} frame={} draw={} segment={} current={} "
+              "aligned={} incoming={} required={} capacity={} reason=draw-preflight-missed",
+              g_recorder.frameSlot, frame.frameId, g_recorder.drawCallCount, frame.storageSegment, current, aligned,
+              length, required, StorageBufferSize);
+    AURORA_ASSERT(false, "storage upload exceeded the active segment after draw preflight");
+  }
+
+  return push(storage, data, length, alignment);
 }

 Range push_texture_data(const uint8_t* data, u32 bytesPerRow, u32 rowsPerImage) {
diff --git a/lib/gfx/recording.hpp b/lib/gfx/recording.hpp
--- a/lib/gfx/recording.hpp
+++ b/lib/gfx/recording.hpp
@@ -78,6 +78,10 @@ Range push_uniform(const T& data) {
   return push_uniform(reinterpret_cast<const uint8_t*>(&data), sizeof(T));
 }
 Range push_storage(const uint8_t* data, size_t length);
+size_t storage_size() noexcept;
+size_t storage_capacity() noexcept;
+size_t storage_alignment() noexcept;
+bool rollover_storage_pass(size_t requiredEnd);
 template <typename T>
 Range push_storage(ArrayRef<T> data) {
   return push_storage(reinterpret_cast<const uint8_t*>(data.data()), data.size() * sizeof(T));
diff --git a/lib/gfx/resources.hpp b/lib/gfx/resources.hpp
--- a/lib/gfx/resources.hpp
+++ b/lib/gfx/resources.hpp
@@ -7,7 +7,12 @@ namespace aurora::gfx {
 inline constexpr uint64_t UniformBufferSize = 25165824; // 24 MiB
 inline constexpr uint64_t VertexBufferSize = 5242880;   // 5 MiB
 inline constexpr uint64_t IndexBufferSize = 2097152;    // 2 MiB
-inline constexpr uint64_t StorageBufferSize = 8388608;  // 8 MiB
+// GekkoAOT keeps the GPU-visible storage arena fixed while allowing additional
+// CPU staging segments to be allocated only for unusually storage-heavy frames.
+#ifndef GEKKOAOT_AURORA_STORAGE_BUFFER_SIZE
+#define GEKKOAOT_AURORA_STORAGE_BUFFER_SIZE (64ull * 1024ull * 1024ull)
+#endif
+inline constexpr uint64_t StorageBufferSize = GEKKOAOT_AURORA_STORAGE_BUFFER_SIZE; // 64 MiB per segment
 inline constexpr uint64_t TextureUploadSize = 25165824; // 24 MiB

 namespace detail {
diff --git a/lib/gx/command_processor.cpp b/lib/gx/command_processor.cpp
--- a/lib/gx/command_processor.cpp
+++ b/lib/gx/command_processor.cpp
@@ -383,18 +383,6 @@ static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Rang
   auto& state = g_gxState;
   auto& cache = sDrawCache;

-  DrawImmediateData immediates{.vtxStart = vertRange.offset, .currentPnMtx = state.currentPnMtx};
-  for (int i = GX_VA_POS; i <= GX_VA_TEX7; ++i) {
-    if (state.vtxDesc[i] != GX_INDEX8 && state.vtxDesc[i] != GX_INDEX16) {
-      continue;
-    }
-    auto& array = state.arrays[i];
-    if (array.cachedRange.size == 0) {
-      array.cachedRange = gfx::push_storage(static_cast<const uint8_t*>(array.data), array.size);
-    }
-    immediates.arrayStart[i - GX_VA_POS] = array.cachedRange.offset;
-  }
-
   const u8 lineMode = line_mode_for_prim(prim);
   const auto targetLayoutKey = gfx::get_render_target_layout().key;
   const bool pipelineValid = cache.hasPipeline && (state.dirty & DirtyPipeline) == 0 && cache.fmt == fmt &&
@@ -416,6 +404,65 @@ static void push_gx_draw(GXPrimitive prim, GXVtxFmt fmt, u16 vtxCount, gfx::Rang
     }
   }

+  // Storage ranges are offsets into one GPU-visible storage buffer. GekkoAOT
+  // may back a very heavy guest frame with multiple CPU staging segments, but
+  // a segment transition must happen before a draw so every array/fog offset
+  // used by that draw belongs to the same segment.
+  const size_t storageAlignment = gfx::storage_alignment();
+  const auto appendProjected = [storageAlignment](size_t cursor, size_t bytes) {
+    if (bytes == 0) {
+      return cursor;
+    }
+    if (storageAlignment != 0) {
+      cursor = AURORA_ALIGN(cursor, storageAlignment);
+    }
+    return cursor + bytes;
+  };
+  const auto projectedStorageEnd = [&](size_t cursor) {
+    for (int i = GX_VA_POS; i <= GX_VA_TEX7; ++i) {
+      if (state.vtxDesc[i] != GX_INDEX8 && state.vtxDesc[i] != GX_INDEX16) {
+        continue;
+      }
+      const auto& array = state.arrays[i];
+      if (array.cachedRange.size == 0) {
+        cursor = appendProjected(cursor, array.size);
+      }
+    }
+    if (cache.config.shaderConfig.fogRangeEnabled) {
+      const auto key = fog_range_lut_key();
+      if (!cache.hasFogRange || cache.fogRangeKey != key) {
+        const auto& lut = resolve_fog_range_lut(key);
+        cursor = appendProjected(cursor, lut.size() * sizeof(f32));
+      }
+    }
+    return cursor;
+  };
+
+  size_t projectedEnd = projectedStorageEnd(gfx::storage_size());
+  if (projectedEnd > gfx::storage_capacity()) {
+    if (!gfx::rollover_storage_pass(projectedEnd)) {
+      FATAL("unable to roll Aurora storage staging before draw: required={}, capacity={}", projectedEnd,
+            gfx::storage_capacity());
+    }
+    projectedEnd = projectedStorageEnd(gfx::storage_size());
+    if (projectedEnd > gfx::storage_capacity()) {
+      FATAL("single GX draw requires {} bytes of Aurora storage (segment capacity {})", projectedEnd,
+            gfx::storage_capacity());
+    }
+  }
+
+  DrawImmediateData immediates{.vtxStart = vertRange.offset, .currentPnMtx = state.currentPnMtx};
+  for (int i = GX_VA_POS; i <= GX_VA_TEX7; ++i) {
+    if (state.vtxDesc[i] != GX_INDEX8 && state.vtxDesc[i] != GX_INDEX16) {
+      continue;
+    }
+    auto& array = state.arrays[i];
+    if (array.cachedRange.size == 0) {
+      array.cachedRange = gfx::push_storage(static_cast<const uint8_t*>(array.data), array.size);
+    }
+    immediates.arrayStart[i - GX_VA_POS] = array.cachedRange.offset;
+  }
+
   const bool bindGroupsValid =
       (state.dirty & DirtyTextures) == 0 && cache.bindGeneration == texture::current_bind_generation();
   if (!bindGroupsValid) {
