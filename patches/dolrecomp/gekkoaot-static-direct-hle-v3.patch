diff --git a/src/backend/llvm/exits.cpp b/src/backend/llvm/exits.cpp
index 1ff39c8..49cbf25 100644
--- a/src/backend/llvm/exits.cpp
+++ b/src/backend/llvm/exits.cpp
@@ -6,2 +6,3 @@
 #include <fstream>
+#include <sstream>
 #include <string>
@@ -19,5 +20,10 @@ using namespace llvm;
 namespace {
+struct StaticIntercept {
+  u32 address = 0;
+  u32 direct_token = 0;
+};
+
 struct StaticInterceptTable {
   bool enabled = false;
-  std::vector<u32> addresses;
+  std::vector<StaticIntercept> entries;
 };
@@ -26,4 +32,5 @@ const StaticInterceptTable& staticInterceptTable() {
   // One immutable table per DolRecomp invocation. The manifest is generated by
-  // GekkoAOT's structural SDK resolver before the optimized AOT pass, so this
-  // is compiler input, not runtime game-specific logic.
+  // GekkoAOT's structural SDK resolver before the optimized AOT pass. v70 also
+  // gives proven native entries a stable synthetic host-call token so LLVM can
+  // lower the positive interception path at compile time.
   static const StaticInterceptTable table = [] {
@@ -41,11 +48,30 @@ const StaticInterceptTable& staticInterceptTable() {
         continue;
+      std::istringstream row(line);
+      std::string address_text, name, token_text;
+      if (!(row >> address_text >> name))
+        return StaticInterceptTable{};
       char* end = nullptr;
-      const unsigned long value = std::strtoul(line.c_str(), &end, 0);
-      if (end == line.c_str() || value > 0xfffffffful)
-        return StaticInterceptTable{}; // Malformed input: preserve dynamic checks.
-      result.addresses.push_back(static_cast<u32>(value));
+      const unsigned long address = std::strtoul(address_text.c_str(), &end, 0);
+      if (end == address_text.c_str() || *end != '\0' || address > 0xfffffffful)
+        return StaticInterceptTable{};
+      u32 token = 0;
+      if (row >> token_text) {
+        end = nullptr;
+        const unsigned long parsed = std::strtoul(token_text.c_str(), &end, 0);
+        if (end == token_text.c_str() || *end != '\0' || parsed > 0xfffffffful)
+          return StaticInterceptTable{};
+        token = static_cast<u32>(parsed);
+      }
+      result.entries.push_back({static_cast<u32>(address), token});
     }
-    std::sort(result.addresses.begin(), result.addresses.end());
-    result.addresses.erase(std::unique(result.addresses.begin(), result.addresses.end()),
-                           result.addresses.end());
+    std::sort(result.entries.begin(), result.entries.end(),
+              [](const StaticIntercept& a, const StaticIntercept& b) {
+                return a.address < b.address;
+              });
+    result.entries.erase(
+        std::unique(result.entries.begin(), result.entries.end(),
+                    [](const StaticIntercept& a, const StaticIntercept& b) {
+                      return a.address == b.address;
+                    }),
+        result.entries.end());
     return result;
@@ -55,8 +81,18 @@ const StaticInterceptTable& staticInterceptTable() {
 
-bool regionNeedsInterception(u32 start, u32 end) {
+std::vector<StaticIntercept> regionInterceptions(u32 start, u32 end) {
+  std::vector<StaticIntercept> result;
   const auto& table = staticInterceptTable();
   if (!table.enabled || start >= end)
-    return true;
-  const auto it = std::lower_bound(table.addresses.begin(), table.addresses.end(), start);
-  return it != table.addresses.end() && *it < end;
+    return result;
+  auto it = std::lower_bound(
+      table.entries.begin(), table.entries.end(), start,
+      [](const StaticIntercept& entry, u32 value) { return entry.address < value; });
+  for (; it != table.entries.end() && it->address < end; ++it)
+    result.push_back(*it);
+  return result;
+}
+
+bool regionNeedsInterception(u32 start, u32 end) {
+  const auto entries = regionInterceptions(start, end);
+  return !staticInterceptTable().enabled || !entries.empty();
 }
@@ -125,9 +161,51 @@ void FunctionEmitter::emitEntry() {
     builder_.CreateBr(query);
-  } else if (!regionNeedsInterception(source_.guest_start, source_.guest_end)) {
-    builder_.CreateBr(nativeEntry);
   } else {
-    Value *hostCall = loadOffset(pointer, offsetof(CPUState, host_call));
-    Value *noInterception = builder_.CreateCall(
-        expect, {builder_.CreateIsNull(hostCall), builder_.getTrue()});
-    builder_.CreateCondBr(noInterception, nativeEntry, query);
+    const auto staticEntries =
+        regionInterceptions(source_.guest_start, source_.guest_end);
+    const bool allDirect =
+        !staticEntries.empty() &&
+        std::all_of(staticEntries.begin(), staticEntries.end(),
+                    [](const StaticIntercept& entry) {
+                      return entry.direct_token != 0u;
+                    });
+
+    // v70: for a region whose SDK intercepts all have compile-time service
+    // tokens, dispatch exact intercepted entry PCs directly to CPUState::host_call.
+    // Other entries in the same compiled region no longer pay the old
+    // ppc_native_region_available() round-trip merely because an SDK function
+    // happens to share the region.
+    if (allDirect) {
+      auto* directSwitch = builder_.CreateSwitch(
+          entry_pc_, nativeEntry, static_cast<unsigned>(staticEntries.size()));
+      auto* hostCallType = FunctionType::get(
+          Type::getInt1Ty(context_),
+          {pointer, Type::getInt32Ty(context_)}, false);
+      for (const auto& entry : staticEntries) {
+        BasicBlock* direct =
+            BasicBlock::Create(context_, "static_hle_direct", function_);
+        directSwitch->addCase(
+            ConstantInt::get(Type::getInt32Ty(context_), entry.address), direct);
+        builder_.SetInsertPoint(direct);
+        Value* hostCall = loadOffset(pointer, offsetof(CPUState, host_call));
+        BasicBlock* invoke =
+            BasicBlock::Create(context_, "static_hle_invoke", function_);
+        builder_.CreateCondBr(builder_.CreateIsNull(hostCall), query, invoke);
+        builder_.SetInsertPoint(invoke);
+        Value* handled = builder_.CreateCall(
+            hostCallType, hostCall,
+            {ctx_, builder_.getInt32(entry.direct_token)});
+        BasicBlock* accepted =
+            BasicBlock::Create(context_, "static_hle_accepted", function_);
+        builder_.CreateCondBr(handled, accepted, query);
+        builder_.SetInsertPoint(accepted);
+        returnFromBody();
+      }
+    } else if (!regionNeedsInterception(source_.guest_start, source_.guest_end)) {
+      builder_.CreateBr(nativeEntry);
+    } else {
+      Value *hostCall = loadOffset(pointer, offsetof(CPUState, host_call));
+      Value *noInterception = builder_.CreateCall(
+          expect, {builder_.CreateIsNull(hostCall), builder_.getTrue()});
+      builder_.CreateCondBr(noInterception, nativeEntry, query);
+    }
   }
